# convert a type name to a readr convert code

readr uses convert codes - see vignette("readr") this function allows
for named types and will convert those to the 1-letter codes used by
reader

## Usage

``` r
type_code_to_readr_code(type_code)
```

## Arguments

- type_code:

  single letter or string of type

## Value

character single letter code
