# combine data csvs

given a list of CSV files, read and check the specs, then aggregate into
a single DF.

## Usage

``` r
aggregate_csvs(csv_list, spec.df)
```

## Arguments

- csv_list:

  list of files,e.g.
