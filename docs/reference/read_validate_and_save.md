# given a URL and params, read, validate and save a CSV

filename \<- read_and_save(url, sheet_id = 'biomass_data', spec.df =
commassembly_rules_biomass_str))

## Usage

``` r
read_validate_and_save(url, tab_name, spec.df, csv_folder = "../L0")
```
